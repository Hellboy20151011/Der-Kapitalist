Login.gd

Fehler bei (50, 39): Unexpected "?" in source. If you want a ternary operator, use "truthy_value if true_condition else falsy_value".

status_label.text = (register ? "Registrierung fehlgeschlagen: " : "Login fehlgeschlagen: ") + msg