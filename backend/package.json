{
  "name": "der-kapitalist-backend",
  "version": "1.0.0",
  "description": "Backend API for Der-Kapitalist mobile game",
  "type": "module",
  "main": "src/Server.js",
  "scripts": {
    "start": "node src/Server.js",
    "dev": "node --watch src/Server.js",
    "migrate": "echo 'Run migrations manually: psql -d der_kapitalist -f migrations/XXX.sql'",
    "lint": "echo 'No linter configured yet. Consider adding ESLint.'",
    "format": "echo 'No formatter configured yet. Consider adding Prettier.'"
  },
  "keywords": ["game", "api", "express"],
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.22.4",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  }
}
